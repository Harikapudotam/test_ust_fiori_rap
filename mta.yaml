_schema-version: "3.2"
ID: travusthptravelproc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travusthptravelproc-destination-content
  type: com.sap.application.content
  requires:
  - name: travusthptravelproc-destination-service
    parameters:
      content-target: true
  - name: travusthptravelproc-repo-host
    parameters:
      service-key:
        name: travusthptravelproc-repo-host-key
  - name: travusthptravelproc-uaa
    parameters:
      service-key:
        name: travusthptravelproc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travusthptravelproc_html_repo_host
          ServiceInstanceName: travusthptravelproc-html5-srv
          ServiceKeyName: travusthptravelproc-repo-host-key
          sap.cloud.service: travusthptravelproc
        - Authentication: OAuth2UserTokenExchange
          Name: travusthptravelproc_uaa
          ServiceInstanceName: travusthptravelproc-xsuaa-srv
          ServiceKeyName: travusthptravelproc-uaa-key
          sap.cloud.service: travusthptravelproc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travusthptravelproc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travusthptravelproc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travusthptravelproc.zip
      name: travusthptravelproc
      target-path: resources/
- name: travusthptravelproc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travusthptravelproc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travusthptravelproc-destination-service
    service-plan: lite
- name: travusthptravelproc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travusthptravelproc-xsuaa-srv
    service-plan: application
- name: travusthptravelproc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travusthptravelproc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
